<div class="featured-products">
  <h1 class="subtitle">Featured Products</h1>
  <% products.each_with_index do |product, index| %>
    <%= render 'featured_product', product: product, alignment: (index%2).even? ? "left" : "right" %>
  <% end %>
</div>

