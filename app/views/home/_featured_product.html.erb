<%= content_tag :div, class: ["featured-product", alignment] do %>
  <%= image_tag product.product_image.url(:original) %>

  <div class="text-area">
    <%= product.description %>
  </div>
<% end %>
